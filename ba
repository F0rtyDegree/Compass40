# Скрипт для сборки релиза APK
echo "$(date) сборка APP ..." >> sizes.log

./clear

# flutter clean 
# flutter pub get --offline

flutter test --no-pub
flutter build apk --release --no-pub
# flutter build apk --release --no-pub 2>&1 | grep -v "^Running Gradle task"

mv /home/user/myapp/build/app/outputs/flutter-apk/app-release.apk Compass40.apk

du -h --max-depth=1 /home/user/ | sort -rh >> sizes.log
du -h --max-depth=1 /home/user/.gradle | sort -rh >> sizes.log
du -h --max-depth=1 /home/user/.gradle/daemon | sort -rh >> sizes.log
du -h --max-depth=1 /home/user/.gradle/caches | sort -rh >> sizes.log
du -h --max-depth=1 /home/user/.gradle/caches/8.12 | sort -rh >> sizes.log
df -h .  >> sizes.log
df -h .

echo "$(date) сборка завершена" >> sizes.log
echo "" >> sizes.log

#!/bin/bash
